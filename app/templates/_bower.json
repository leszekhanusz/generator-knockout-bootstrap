{
  "name": "<%= _.slugify(appname) %>",
  "dependencies": {
    "requirejs": "^2.1.11",
    "jquery": "^2.1.0",
    "knockout.js": "^3.0.0",<% if (includeSammy) { %>
    "sammy": "^0.7.5",<% } %><% if (includeFontAwesome) { %>
    "font-awesome": "^4.0.3",<% } %><% if (includeSocketIO) { %>
    "socket.io-client": "^0.9.16",<% } %><% if (includeValidation) { %>
    "knockout.validation": "^1.0.2",<% } %><% if (includeBootbox) { %>
    "bootbox": "^4.2.0",<% } %><% if (includeNotify) { %>
    "notifyjs-dist": "^0.3.2",<% } %>
    "bootstrap-sass-official": "^3.2.0"
  }
}

